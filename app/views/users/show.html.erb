<%= render 'shared/header', title: "Two Tone Twitter" %>

<% if current_user.id == @user.id %>
  <%= link_to "Tweet away!", new_user_tweet_path(@user), id: "new_tweet_link", remote: true %>
<% end %>

<%= render "tweets" %>
